# Leave empty for Vite proxy in local dev (vite.config.ts → localhost:8080)
# For a separately hosted frontend, set the public gateway URL:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
